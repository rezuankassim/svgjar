<div>
    <a href="{{ $link }}" class="flex items-center space-x-4 rounded-md px-2 py-2 hover:bg-gray-100 {{ $active ? 'text-indigo-500' : '' }}">
        {{ $icon }}

        {{ $title }}
    </a>
</div>